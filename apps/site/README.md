# comb.is

The public landing page for the Comb entitlement protocol.

The page uses a scroll-driven handshake to show the protocol boundary: payment
and durable membership state remain with an Issuer while a narrow, signed
entitlement travels to a trusted Consumer.

## Local development

```bash
npm install
npm run dev
npm test
```
